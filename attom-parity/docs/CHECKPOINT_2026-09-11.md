# SIGNAL$ × ATTOM parity — CHECKPOINT, Fri Sep 11 2026 7:20 PM ET (rows updated Sat Sep 12 11:15 PM ET — K16 CLOSED 10:13 AM; row()/twoCol() follow-up 01d4eb35 shipped 6:44 PM via #20423; K6 foreclosure candidate fc-sold3p-po-v1 recorded, still WITHHELD; K1/K2/K5 re-queried 11:11 PM; battle card index.html + BATTLE_CARD.md refreshed to the lane outcomes. Note: this file lives in everest-battle-cards and biddeed-attom-agent-reports; cli-anything-biddeed never had a docs/ copy until 11:15 PM ET 9/12 — the board #20287 comments were its record)

Board of record: cli-anything-biddeed #20287. Every number below was re-queried at 7:16 PM ET (Supabase mocerqjnksmhcjzxrewo) or read from `main`; nothing is carried from a run badge or an issue comment. Status vocabulary: HAVE · PARTIAL · GAP · BLOCKED · OURS (no ATTOM twin) · DEFECT.

## Closed today (for the record — not open)

E1 §REHAB renderer (b89233f9) · B1 Layer 3 re-land (50f92d72; 12,775 + 5,452 rows) · D POI (a993c776 + fix 2b22f957, verified against live OSM) · E schools nearest (99c5aee4) · G executive summary (7eba5097) · defects E1–E7 · HTML twin parity (5f141e36 → 02a8938d, deployed) · pdf.js/twin kill-list gate (0fceac18 / a9cf3972, other lane) · tax-deed model `td-soldvred-v1` validated and wired (74ca54fc, other lane) · battle card + private repos + /answers embed live · Cards Complete = 3.

## OPEN KPIs — one row per gap, with the live number and what closes it

| # | KPI (ATTOM family → SIGNAL$ section) | Status | Live evidence 7:16 PM ET | What moves it | Owner |
|---|---|---|---|---|---|
| K1 | Tax amount / paid-delinquent status (A4 → §16 tax block, title CP3) | **BLOCKED** | `tax_collector_results` 66 attempts, **66 blocked, 0 ok** (unchanged at 11:11 PM ET 9/12; sources 35 blocked / 5 n/a); 13 Cloudflare Turnstile challenges; `title_search_snapshot.tax.payment_status` prints "Pending — tax collector not reachable"; CP3 statewide **0.86** unchanged | A decision, not code: (a) leave Pending (honest, current), (b) annual published delinquent-tax lists per county → "delinquent yes/no" only, no current amount, (c) a residential-IP fetch path — spend + a rule you set (no proxies / no captcha bypass) | **Ariel** |
| K2 | Title parity — vesting, chain, mortgages, encumbrances, lien priority (A2, A5 → §16) | **PARTIAL** | `v_title_parity_kpi_statewide` (computed 4:15 PM ET): (re-computed 6:39 AM ET 9/12) 1,922 upcoming / 59 counties / avg parity **35%** / **68 at parity (3.5%)**; CP1 0.76 · CP2 1.03 · CP3 0.91 · CP4 0.54 · CP5 0.50 · CP6 0.45; `title_search_snapshots` = 3 | The 20 Turnstile counties stay deferred (your call, earlier). CP4/CP5/CP6 rise only with the OR harvest lanes already running under the shared cc lease (#20255 + shards) — not an ATTOM-kit item | CC lanes (running) |
| K3 | POI rendered on a real report (A8 → §11-14) | **HAVE** (closed 8:20 PM ET) | Three real renders since the fix filled `context_layer_cache` poi: 32904 → Winn-Dixie 0.97 mi, 4 parks (Max K Rodes Park 0.48 mi), 3 schools (West Melbourne Elementary 0.85 mi — matches the NCES row at 0.84 mi); 33472 → Publix 0.79 mi, Christa McAuliffe Middle 0.99 mi; 34653 → Connerton Elementary 0.69 mi. Named real places, honest zeros elsewhere | — | closed |
| K4 | Assigned school (SABS) printed (A9 → §11-14) | **HAVE** (closed 8:50 PM ET) | pdf.js 77bd4004 and S5Report.tsx 44e8f124 print "Assigned Schools — Elementary: … · Middle: … · High: …" from `context_layers.schools.assigned`, Pending reasons verbatim ("no boundary published" / "multiple overlapping boundaries") | — | closed |
| K5 | Layer 3 appreciation / inventory rendered (A6, B1, B2 → §4-7 Layer 3 block) | **PARTIAL → HAVE pending a render** | tables present (12,775 / 5,452 rows, RLS on, 0 anon); composer references `cma_layer3` + `insurance_kpi`; no report rendered since the re-land (`s5_pdf_cache` last row Sep 11 12:17 PM ET — re-checked 11:11 PM ET 9/12, still the last row; `report_sha256` populated on 0 rows) | First real report showing the Layer 3 block with a labelled geography | Architect (next render) |
| K6 | Foreclosure ML probability (D → §ML) | **OURS — WITHHELD · decision** | `shapira_model_validations` re-queried 11:11 PM ET 9/12: V4 prod OOT AUC **0.478**, v5b **0.63**, v5 logreg **0.59**; NEW `fc-sold3p-po-v1` (PR #20395 merged 1:04 AM ET 9/12): AUC **0.8765** vs 0.8291 baseline, n_test 4,655, labels derived (winbid>maxbid) with a 59-row verified chain (97.5% positive precision), top-decile precision 0.847. Its notes cite an owner override "ML beats by 0.01 AUC at least" (1:00 AM ET 9/12) — but the DB `passed` column is GENERATED on the +0.10-lift term → **passed=false**; the composer gate queries `passed=eq.true`; composer.js has zero references to the model. Net: foreclosure prints WITHHELD in all three renderers exactly as before. Tax deed unchanged: `td-soldvred-v1` passed and prints | **Ariel**: (a) confirm or deny the +0.01 override, (b) say whether derived labels satisfy "verified outcomes". If both yes → migration on the generated column (production schema change, needs your approval) + composer wiring; if not → #20240 rebuild on verified labels | **Ariel**, then ML lane |
| K7 | Whole-report sha256 at purchase (D → §17 provenance) | **HAVE** (shipped b1739b06, 7:36 PM ET) | `s5_pdf_cache.report_sha256` column live (migration 20260911h); `stripe-webhook/report-hash.js` hashes canonical JSON (keys sorted, `provenance.snapshot_sha256` nulled first) at purchase; pdf.js §17 row "Report snapshot sha256"; HTML twin row 38e75b19. Known limit, by construction: the PDF bytes are rendered before the hash exists, so the PDF row prints "Pending — hashed at purchase" and the hash lives in `report_json` / the HTML twin | Optional later: re-render after hashing so the PDF carries it | closed |
| K8 | ZoneWise district per county (D → §ZW) | **OURS / PARTIAL** | DOR land use + just value live; district assignment prints "PENDING — district layer not built" outside the built counties | GIS mission #20246 (running, separate program) | CC lane (running) |
| K9 | Maps / aerial in the report (A10, C3) | **HAVE** (shipped 218b6a05, 8:38 PM ET — retry; first fully green cc-runner run of the day) | Attempt 1 produced zero file changes (session "ended its turn" waiting for a wakeup `-p` never delivers; runner patched 9c71a183). Retry: `aerial.js getSubjectAerial` reads the SAME stored aerial the deal page renders (`winnerdata.biddeed_reels.aerial_tight_url` via a new SECURITY DEFINER accessor `get_biddeed_reel_aerial`, granted to service_role only; keyed on case_number + county because the reels table has no mca_id); composer `property_record.aerial`; pdf.js `drawAerialThumbnail` with buffer fetch + Pending fallback "Pending — image not available at generation time"; 232-line test. Architect fix 673384ea: the customer caption printed an internal table/column name — now "satellite imagery on file for this case" (test updated 163b725e). HTML twin `<img>` mirror 44e8f124. Missing from DoD: docs/spec/20337.md and the two verification PNGs | none blocking; spec + PNGs can follow | closed |
| K10 | Charts in the report (B6, C6) | **HAVE** (shipped 64ae3bd3, 7:50 PM ET) | `priors.getClearingRatioHistory`: same county + SAME sale type, trailing 24 months, months with n ≥ 10 only, ≥ 6 months or `{available:false, reason}`; pdf.js vector sparkline in SSOT colours with min/max/latest + n range caption; verification PNGs (Orange foreclosure, Pasco tax deed); HTML twin inline-SVG sparkline 38e75b19. Note from the lane: pdfkit's WinAnsi font has no "≥" glyph (rendered as "e10"), so captions use ">=" | C7 generative UI (#19947) remains the wider item | closed |
| K11 | Rental estimate + value history (C5 → §2-3) | **HAVE** (shipped 50029cc9, 7:55 PM ET) | composer `value_estimate.rental_estimate` (median `rent_price`, same zip + bedrooms, 180 days, n ≥ 5, zip→county fallback, else "Pending — fewer than 5 comparable rental listings") and `value_history` from `fl_parcels.jv` / `jv_chng` (two points; "longer history: Pending" printed verbatim); pdf.js rows + new test file; HTML twin rows 38e75b19 | none | closed |
| K12 | Investor activity depth (B3 → §12) | **OURS / PARTIAL** | 21,138 verified outcomes; winner names from the Bid History harvest (#19446) still thin | Outcome harvest lanes (separate program) | CC lanes (running) |
| K13 | Other hazards — wind / wildfire / sinkhole (A7) | **GAP by decision** | prints "Pending — not sourced" | Only if a customer asks; FEMA flood is live | — |
| K14 | Occupancy (A2) | **GAP by design** | no source; homestead status is the printed proxy | None — no homeowner-data purchase (compliance rule) | — |
| K15 | Rehab estimate engine (§REHAB) | **PARTIAL** | both renderers print "Pending — rehab estimate engine not yet producing for this parcel"; no engine | Pro Plus construction-management program (C12) feeds this, not the ATTOM kit | separate program |
| K16 | pdf.js layout defects (flagged by the kill-list lane) | **CLOSED — 5/5 VERIFIED on real pixels** (e22b17fe → c3f11703 → 34bab415, 10:13 AM ET 9/12) | Lane #20419 rendered a real captured foreclosure (Lee 25-CA-007067) across two runs; I pulled and looked at every PNG myself, not just the lane's claim. #1/#2 (cover KPI strip) PASS — verdict text inside the band, exec-summary not overlapping. #4 (§18 legacy keys) PASS — real Result/Sale Amount/Winning Bidder/Clearing Multiple/Value Band Call print for the captured case. #5 (row() page-break) PASS — no stray near-empty page. #3 (flagBox): first pass fixed the same-baseline overlap but missed a wrapped-text adjacent-flag overlap (MECHANIC_LIEN_RISK's 3-line detail vs FEDERAL_LIENS's header); follow-up c3f11703 measures real height via doc.heightOfString() — re-verification PNG confirms it: MECHANIC_LIEN_RISK's full 3-line detail now sits inside its own correctly-sized box and FEDERAL_LIENS starts clean below with visible whitespace, no collision (I viewed the crop myself). Bonus: found+fixed the cover's ▲ glyph (no WinAnsi mapping, rendered as garbage "%²") while looking at the pixels, commit 34bab415 — same bug class as the documented ≥ → "e10" issue from K10. git diff confirms zero renderer files touched by either CC lane run — only new verification/spec files each time | closed | Architect + CC lane #20419 |
| K17 | Grok GitHub write (Mission 0) | **BLOCKED — your side** | both connectors 403 on every write; Grok cannot push | 6 steps in docs/PRIVATE_REPO_HANDOFF.md (GitHub App installation permissions); until then Grok drafts, cc-runner executes | **Ariel** |
| K18 | biddeed.ai homepage tile for the /answers embed | **OPTIONAL** | page is live and DB-driven; no homepage entry point | biddeed-web PR + your merge | Ariel (merge) |
| — | row()/twoCol() wrapped-text overlap (§17/§18) — the K16 pattern's last two sites | **CLOSED** (01d4eb35, 6:44 PM ET 9/12, issue #20423) | the lane titled it "K18" — number collision with the tile row above; the tile keeps K18 | — | closed |

## Found while verifying (8:20 PM ET) — two RLS-gate regressions from other lanes, closed

The cc-runner RLS gate was no longer a pure false-red: it reported **2 new violations** vs the 307-object baseline — `public.v_td_sale_probability` (definer-semantics view with default anon/authenticated grants → the tax-deed model output was readable by anon through PostgREST; created 6:31 PM ET by the TD-model lane) and `public.biddeed_chat_identity_codes` (policy declared for role public; functionally closed by its `service_role` qual, but a verification-code table with a public-role policy). Both closed by tracked migration `rls_gate_regressions_td_view_chat_identity_20260912`: `security_invoker = true` + anon/authenticated revoked on the view (service role still reads 5,380 rows), policy scoped to `service_role` + grants revoked on the codes table. Behaviour-identical for the report engine and the chat flow.

## Decisions that are yours (everything else runs without asking)

1. **K1 tax collector** — leave Pending, published delinquent lists (yes/no only), or a paid residential-IP path (breaks your no-proxy rule).
2. **K17 Grok write** — the GitHub App permission grant.
3. **K18 homepage tile** — merge or skip.
4. **K6 foreclosure override** — confirm or deny the "+0.01 AUC lift" rule the fc-sold3p-po-v1 validation row cites, and whether derived labels count as verified outcomes. The DB gate still enforces +0.10, so nothing prints until you rule.

## What the architect runs next without asking

K5 verification on a rendered report (none since Sep 11 12:17 PM ET). K3, K4, K7, K9, K10, K11, K16 and the #20423 follow-up are closed. The moat sections (two bands, SIGNAL$ Max Bid, statute-cited title stack, §18 writeback) already render.

Pairing rule holds: BidDeed.AI + ZoneWise.AI together. Vendor name count in customer output: 0. Honesty Protocol: every open row above prints Pending / UNRESOLVED / WITHHELD in-line today.
